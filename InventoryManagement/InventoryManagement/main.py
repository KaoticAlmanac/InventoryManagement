from getpass import getpass

#This fills in the data we need
def initiateItemization():
    UserAuth()

    return
def UserAuth():
    username=raw_input("Please Enter Your Username\n")
    password=getpass()
    #TODO have it log into server and access that
    ##Currently having the username and pass don't matter
    #######
    #login = LoginAuth()
    #bac=login.loginHandler(username,password)
    #if bac != "Failed" go through
    #######
    return